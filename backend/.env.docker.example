# Port the backend server will run on
PORT=5001

# Docker DB URL — host is the name of the service in docker-compose.yml
DATABASE_URL=postgresql://postgres:your_docker_password@db:5432/ticket_app

# Secret used to sign JWT tokens
jwtSecret=your_jwt_secret_here
